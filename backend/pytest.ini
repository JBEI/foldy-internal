[pytest]
minversion = 6.0
addopts = -v -s -m "not slow"
pythonpath = .
testpaths =
    app/tests
    folde/tests
markers =
    slow: benchmark/perf tests that are not part of the normal fast suite; run explicitly with `-m slow`.
